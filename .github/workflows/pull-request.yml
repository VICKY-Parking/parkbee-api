name: Pull request

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check API and AUTH URLs
        run: |
          echo "AUTH_URL is ${{ vars.AUTH_URL }}"
          echo "API_URL is ${{ vars.API_URL }}"
        
      - name: Create Environment Variables
        run: |
          touch .env
          echo AUTH_URL=${{ vars.AUTH_URL }} >> .env
          echo API_URL=${{ vars.API_URL }} >> .env
          echo PPM_CLIENT_ID=${{ vars.PPM_CLIENT_ID }} >> .env
          echo PPM_CLIENT_SECRET=${{ secrets.PPM_CLIENT_SECRET }} >> .env
          echo PREPAID_CLIENT_ID=${{ vars.PREPAID_CLIENT_ID }} >> .env
          echo PREPAID_CLIENT_SECRET=${{ secrets.PREPAID_CLIENT_SECRET }} >> .env
      
      - name: Cat Environment Variables
        run: |
          cat .env
        
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 16

      - name: Install Dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Test
        run: npm run test
